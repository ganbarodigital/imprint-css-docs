---

const { name, text } = Astro.props;

import imprintConfig from "../../imprint.config";

---
<span class="inline external-link"><a href={imprintConfig.externalLinks[name]}>{text ? <span>{text}</span>:<span>{name}</span>}</a></span>